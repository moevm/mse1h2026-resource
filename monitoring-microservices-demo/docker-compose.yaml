services:
  # Database to store traces by Grafana Labs
  tempo:
    image: grafana/tempo:2.6.1
    command: [ "-config.file=/etc/tempo.yaml" ]
    volumes:
      - ./etc/tempo/tempo.yaml:/etc/tempo.yaml
      - tempo-data:/var/tempo
    # ports:
      # - "3200:3200"     # tempo
      # - "9095:9095"     # tempo grpc
      # - "4317:4317"     # otlp grpc
      # - "4318:4318"     # otlp http
    # depends_on:
    #   - init

  # Jaeger
  jaeger:
    image: jaegertracing/all-in-one:1.64.0
    volumes:
      - ./etc/jaeger/jaeger-ui.json:/etc/jaeger/jaeger-ui.json
    command:
      - --query.ui-config=/etc/jaeger/jaeger-ui.json
    environment:
      - COLLECTOR_OTLP_ENABLED=true
      - COLLECTOR_OTLP_GRPC_HOST_PORT=:4317
      - COLLECTOR_OTLP_HTTP_HOST_PORT=:4318
      - METRICS_STORAGE_TYPE=prometheus
      - PROMETHEUS_SERVER_URL=http://prometheus:9090
      - LOG_LEVEL=debug
    ports:
      - "16686:16686"  # Jaeger UI accessed by http://localhost:16686
      # - "14269:14269"  # health check at / and metrics at /metrics
      # - "4317:4317"    # OTLP gRPC receiver

  # And put them in an OTEL collector pipeline...
  otel-collector:
    # image: otel/opentelemetry-collector-contrib:latest
    image: otel/opentelemetry-collector:0.122.0
    command: [ "--config=/etc/otel-collector.yaml" ]
    volumes:
      - ./etc/otel-collector/config.docker.yaml:/etc/otel-collector.yaml
      # - ./etc/otel-collector/simple-debug.yaml:/etc/otel-collector.yaml
    ports:
      # - "1888:1888"   # pprof extension
      # - "8888:8888"   # Prometheus metrics exposed by the collector
      # - "8889:8889"   # Prometheus exporter metrics
      # - "13133:13133" # health_check extension
      - "4317:4317"   # OTLP gRPC receiver
      - "4318:4318"   # OTLP HTTP receiver
      # - "55679:55679" # zpages extension
    depends_on:
      - tempo
      - jaeger

  loki:
    image: grafana/loki:3.4.2
    command: ["--pattern-ingester.enabled=true", "--config.file=/etc/loki/loki.yaml"]
    ports:
      - "3100:3100"
    volumes:
      - "./etc/loki/loki.yaml:/etc/loki/loki.yaml"

  # As such, we need to chown the volume in order for Tempo to start correctly.
  pyrra-init:
    image: ghcr.io/pyrra-dev/pyrra:v0.7.7
    user: root
    entrypoint:
      - "chown"
      - "65533:65533"
      - "/etc/prometheus/pyrra"
    volumes:
      - prometheus_pyrra:/etc/prometheus/pyrra

  pyrra-sleep:
    image: ghcr.io/pyrra-dev/pyrra:v0.7.7
    entrypoint:
      - "sleep"
      - "5"

  prometheus:
    image: prom/prometheus:v2.55.1
    hostname: localhost
    command:
      - --config.file=/etc/prometheus.yaml
      - --web.enable-remote-write-receiver
      - --web.enable-lifecycle
      - --enable-feature=exemplar-storage
      - --enable-feature=native-histograms
    volumes:
      - ./etc/prometheus/prometheus.yaml:/etc/prometheus.yaml
      - prometheus_pyrra:/etc/prometheus/pyrra
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"

  alertmanager:
    image: prom/alertmanager:v0.28.1
    hostname: localhost
    ports:
      - "9093:9093"
    volumes:
      - ./etc/alertmanager/:/etc/alertmanager/
      - alertmanager-data:/data
    command:
      - '--config.file=/etc/alertmanager/config.yml'
      - '--log.level=debug'

  webhook-tester:
    image: tarampampam/webhook-tester:1.1.0
    command: serve --port 8080
    ports:
      - '8080:8080'
    environment:
      - CREATE_SESSION=00000000-0000-0000-0000-000000000000

  karma:
    image: ghcr.io/prymitive/karma:v0.121
    ports:
      - '8081:8081'
    environment:
      - ALERTMANAGER_URI=http://alertmanager:9093
      - ALERTMANAGER_INTERVAL=10s
      - PORT=8081
    depends_on:
      - alertmanager

  pyrra-api:
    image: ghcr.io/pyrra-dev/pyrra:v0.7.7
    command:
      - api
      - --prometheus-url=http://prometheus:9090
      - --prometheus-external-url=http://localhost:9090
      - --api-url=http://pyrra-filesystem:9444
    ports:
      - "9099:9099"
    depends_on:
      - prometheus

  pyrra-filesystem:
    image: ghcr.io/pyrra-dev/pyrra:v0.7.7
    command:
      - filesystem
      - --log-level=debug
      - --prometheus-url=http://prometheus:9090
      - --generic-rules
    volumes:
      - ./etc/pyrra:/etc/pyrra
      - prometheus_pyrra:/etc/prometheus/pyrra
    depends_on:
      - prometheus
      - pyrra-api
      - pyrra-init

  grafana:
    image: grafana/grafana:11.5.2
    volumes:
      - ./etc/grafana/datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml
      - ./etc/grafana/dashboards.yaml:/etc/grafana/provisioning/dashboards/dashboards.yaml
      - ./etc/grafana/plugins.yaml:/etc/grafana/provisioning/plugins/plugins.yaml
      - ./etc/dashboards:/etc/grafana/dashboards
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_DISABLE_LOGIN_FORM=true
      - GF_FEATURE_TOGGLES_ENABLE=traceqlEditor traceQLStreaming metricsSummary traceToMetrics traceqlSearch
      # - GF_INSTALL_PLUGINS=grafana-lokiexplore-app
      - GF_INSTALL_PLUGINS=grafana-lokiexplore-app, grafana-pyroscope-app
      # - GF_INSTALL_PLUGINS=https://storage.googleapis.com/integration-artifacts/grafana-lokiexplore-app/grafana-lokiexplore-app-latest.zip;grafana-lokiexplore-app
    ports:
      - "13000:3000"
    depends_on:
      - tempo
      - loki
      - pyroscope

  fastapi-app:
    image: fastapi_app
    build:
      context: ./fastapi_app
    environment:
      - OTLP_GRPC_ENDPOINT=http://otel-collector:4317
      - DATABASE_URL=postgresql://user:password@postgres-db:5432/db
      - PYROSCOPE_ENDPOINT=http://pyroscope:4040
      - REDIS_URL=redis://redis:6379/0
    ports:
      - "18000:8000"
    depends_on:
      - otel-collector
      - postgres-db
      - redis

  flask-app:
    image: flask_app
    build:
      context: ./flask_app
    environment:
      - FASTAPI_URL=http://fastapi-app:8000
      - GOLANG_URL=http://golang-app:8002
      - OTEL_SERVICE_NAME=flask-app
      - OTEL_EXPORTER_OTLP_PROTOCOL=grpc
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
      - OTEL_PYTHON_LOGGING_AUTO_INSTRUMENTATION_ENABLED=true
      - OTEL_PYTHON_LOG_CORRELATION=true

      - OTEL_EXPORTER_PROMETHEUS_HOST=0.0.0.0
      - OTEL_EXPORTER_PROMETHEUS_PORT=9464

      - OTEL_TRACES_EXPORTER=otlp
      - OTEL_METRICS_EXPORTER=otlp,prometheus
      - OTEL_LOGS_EXPORTER=otlp

      # - OTEL_TRACES_EXPORTER=console
      # - OTEL_METRICS_EXPORTER=none
      # - OTEL_LOGS_EXPORTER=none
    ports:
      - "8001:8001"
      - "9464:9464"
    depends_on:
      - otel-collector
      - fastapi-app
      - golang-app

  golang-app:
    image: golang_app
    build:
      context: ./golang_app
    environment:
      - OTEL_SERVICE_NAME=golang-app
    ports:
      - "8002:8002"

  postgres-db:
    image: postgres:16-alpine
    volumes:
      # - ./data/postgres/:/var/lib/postgresql/data/
      - ./etc/postgres/:/docker-entrypoint-initdb.d/
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=db
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  pyroscope:
    image: grafana/pyroscope:1.12.0
    command: [ "-config.file=/etc/pyroscope.yml" ]
    ports:
      - "4040:4040"
    volumes:
      - ./etc/pyroscope/pyroscope.yml:/etc/pyroscope.yml

  tempo-watcher:
    build:
      context: ./tempo-watcher
      dockerfile: Dockerfile
    environment:
      - RESOURCE_API_URL=http://resource-backend:8000
      - TEMPO_URL=http://tempo:3200
      - AGENT_NAME=tempo-watcher
      - ADMIN_EMAIL=admin1@example.com
      - ADMIN_PASSWORD=admin
      - APP_NAME=monitoring-demo
      - POLL_INTERVAL=15
      - LAG_SECONDS=15
      - MAX_TRACES=200
      - AGENT_TOKEN_FILE=/data/agent.token
      - LAST_END_FILE=/data/last_end
    volumes:
      - tempo-watcher-data:/data
    depends_on:
      - tempo
    networks:
      - default
      - resource-edge-net

  otel-watcher:
    build:
      context: ./otel-watcher
      dockerfile: Dockerfile.docker
    environment:
      - RESOURCE_API_URL=http://resource-backend:8000
      - AGENT_NAME=otel-watcher
      - ADMIN_EMAIL=admin1@example.com
      - ADMIN_PASSWORD=admin
      - APP_NAME=monitoring-demo
      - SCRAPE_TARGETS=fastapi-app:8000,flask-app:9464,otel-collector:8889,golang-app:8002,beyla:8999
      - SCRAPE_INTERVAL=30
      - AGENT_TOKEN_FILE=/data/agent.token
    volumes:
      - otel-watcher-data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - otel-collector
      - fastapi-app
      - flask-app
    ports:
      - "8090:8090"
    networks:
      - default
      - resource-edge-net

  k8s-watcher:
    build:
      context: ./k8s-watcher
      dockerfile: Dockerfile.docker
    environment:
      - RESOURCE_API_URL=http://host.docker.internal:8000
      - AGENT_NAME=k8s-watcher
      - ADMIN_EMAIL=admin1@example.com
      - ADMIN_PASSWORD=admin
      - APP_NAME=monitoring-demo
      - WATCH_NAMESPACE=monitoring-demo
      - WATCH_INTERVAL=30
    extra_hosts:
      - "host.docker.internal:host-gateway"
    profiles:
      - kubernetes

  beyla:
    image: grafana/beyla:2.6.0
    pid: "service:golang-app"
    privileged: true
    environment:
      BEYLA_CONFIG_PATH: /etc/beyla/config.yml
    depends_on:
      - otel-collector
      - golang-app
    ports:
      - "8998:8998" # internal metrics
    volumes:
      - ./etc/beyla/config.yml:/etc/beyla/config.yml

  traffic-generator:
    build:
      context: ./traffic_generator
    environment:
      - FASTAPI_URL=http://fastapi-app:8000
      - FLASK_URL=http://flask-app:8001
      - GOLANG_URL=http://golang-app:8002
    depends_on:
      - fastapi-app
      - flask-app
      - golang-app


volumes:
  prometheus_pyrra: {}
  alertmanager-data: {}
  tempo-data: {}
  prometheus-data: {}
  otel-watcher-data: {}
  tempo-watcher-data: {}

networks:
  default:
  resource-edge-net:
    external: true
    name: resource-edge-net