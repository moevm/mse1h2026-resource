apiVersion: v1
kind: Service
metadata:
  name: webhook-tester
  namespace: monitoring-demo
spec:
  type: LoadBalancer
  selector:
    app: webhook-tester
  ports:
    - name: http
      port: 8080
      targetPort: 8080
