apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring-demo
spec:
  type: LoadBalancer
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
